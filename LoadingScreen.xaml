<Window x:Class="RecoveryApplication.LoadingScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False" Height="250" Width="575">
    <Border CornerRadius="20" Opacity="0.8" Margin="0,0,125,90">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FF817971" Offset="0"/>
                <GradientStop Color="#FF252B30" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Archiving..." FontSize="20" FontWeight="Bold" />
                <ProgressBar IsIndeterminate="True" Width="250" Margin="20" Height="10"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>